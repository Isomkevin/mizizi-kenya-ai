# Copy these lines into your root `.env` for local Docker Neo4j (Option A).
# Matches docker-compose.neo4j.yml defaults.

NEO4J_PROFILE=local
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=mizizi-local-dev
NEO4J_DATABASE=neo4j
