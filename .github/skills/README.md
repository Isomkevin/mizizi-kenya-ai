# GitHub Copilot agent skills

Canonical skill library: [`.agents/skills/`](../.agents/skills/README.md)

When a task matches a skill description, read `.agents/skills/<name>/SKILL.md` before implementing. Do not duplicate skill content here — Copilot discovers skills via this pointer and `AGENTS.md`.
