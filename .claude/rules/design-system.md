---
paths:
  - "src/styles.css"
  - "src/components/**"
---

# Design system

- Tokens in `src/styles.css`; fonts linked in `__root.tsx`.
- shadcn in `src/components/ui/` — customize via CSS variables, no purple/indigo defaults.
- Landing: `src/components/landing/`. App: `src/components/app/`.
- `PlaceholderPanel` for routes not yet built.
